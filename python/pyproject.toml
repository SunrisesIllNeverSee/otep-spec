[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
# Distribution name kept as "sigrank-standard" for PyPI compatibility.
# The package was originally published under this name; renaming would break
# existing installs. The canonical protocol name is TTEOP (see TEP-0001).
name = "sigrank-standard"
version = "0.1.0"
description = "TTEOP (Token Telemetry Evaluation Operator Protocol) v0.1-draft — canonical portable five-metric core for AI operator token-processing efficiency. No dependencies."
readme = "README.md"
license = {text = "Apache-2.0"}
requires-python = ">=3.9"
authors = [
    {name = "SunrisesIllNeverSee"}
]
keywords = [
    "sigrank",
    "signalaf",
    "ai-operators",
    "token-efficiency",
    "yield-cascade",
    "telemetry",
    "benchmark",
    "standard",
    "mcp",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering",
    "Topic :: Software Development :: Libraries",
]

[project.urls]
Homepage = "https://signalaf.com/standard"
Repository = "https://github.com/SunrisesIllNeverSee/otep-spec"
Documentation = "https://github.com/SunrisesIllNeverSee/otep-spec#readme"
"Bug Tracker" = "https://github.com/SunrisesIllNeverSee/otep-spec/issues"

[project.scripts]
sigrank-conformance = "sigrank_standard.conformance:main"

[tool.setuptools]
packages = ["sigrank_standard"]
package-dir = {"" = "."}
